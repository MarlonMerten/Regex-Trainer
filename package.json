{
  "name": "regex-trainer",
  "version": "1.0.0",
  "private": true,
  "description": "Interaktiver Trainer für die unterstützte Python-3.14-Regex-Teilmenge",
  "engines": { "node": ">=20" },
  "scripts": {
    "test": "npm run test:syntax && npm run test:self && npm run test:differential",
    "test:syntax": "for file in app/js/*.js app/sw.js; do node --check \"$file\" || exit 1; done",
    "test:self": "node app/tests/selftest.js",
    "test:differential": "node app/tests/python-differential.js",
    "test:browser": "playwright test",
    "test:all": "npm test && npm run test:browser"
  },
  "devDependencies": {
    "@axe-core/playwright": "4.12.1",
    "@playwright/test": "1.62.1"
  }
}
